import CatInfo from "@/components/cat-info";

export default async function Page({ params }) {
    // const { uid } = await params;

    // const res = await fetch("http://localhost:3005/api/cat_status", { next: { revalidate: 60 } });
    // if (!res.ok) return <div>載入失敗</div>;

    // const all = await res.json();
    // const item = all.find(x => (x.uid) === (uid));
    // if (!item) return <div>找不到角色 (uid: {uid})</div>;
    // console.log("page抓到的資料:", all)
    // return (
    //     <div className="w-full md:w-4/6">
    //         <CatInfo data={item} />
    //     </div>
    // );
}

